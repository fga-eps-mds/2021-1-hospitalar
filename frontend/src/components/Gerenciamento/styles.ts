import { makeStyles } from '@material-ui/core'

export const useStyles = makeStyles((theme) => ({
  botaovoltar: {
    alignItems: 'center',
    marginTop: '50px',
  },

  texto_qualificacao: {
    marginTop: '20px',
    marginLeft: '20px',
  },
}))
