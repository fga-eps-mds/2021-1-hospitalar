version: '3'

services:
  frontend:
      build:
        context: ./frontend
        dockerfile: Dockerfile
      command: npm start
      ports:
            - "3000:3000"
      volumes: 
          - .:/usr/app   
      

    


